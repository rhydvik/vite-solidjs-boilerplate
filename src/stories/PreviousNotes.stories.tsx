import { PreviousNotes } from '../views/order/components/notes';

export default {
  title: 'Components/PreviousNotes',
  component: PreviousNotes,
};

const Template = () => <PreviousNotes />;

export const Default = Template.bind({});

Default.args = {};
